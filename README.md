# PersonalDotFiles
It's a personal dotfile i created for my arch linux

On hyprland.conf write:
# This creates a blur behind the Waybar specifically
  blurls = waybar

to restart the waybar
killall waybar && waybar &
